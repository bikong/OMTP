This toolkit includes the MATLAB implementation of grid search, particle swarm optimization, genetic algorithm and gravitational search algorithm for parameters tuning in Support Vector Machine(SVM). Each algorithm has been implemented in two forms: Double Parameters Simultaneous Optimization(error penalty parameters c, kernel parameter g) and Triple Parameters Simultaneous Optimization(error penalty parameters c, kernel parameter g, insensitive loss function parameter p).


================================CG_SVM=======================================
%% psoSVMcg: particle swarm optimization for tuning parameters c,g in SVM.
%% gaSVMcg: genetic algorithm for tuning parameters c,g in SVM. 
%% gsSVMcg: grid search algorithm for tuning parameters c,g in SVM. 
%% gsaSVMcg: gravitational search algorithm for tuning parameters c,g in SVM.


================================CGP_SVM======================================
%% psoSVMcgp: particle swarm optimization for tuning parameters c,g,p in SVM.
%% gaSVMcgp: genetic algorithm for tuning parameters c,g,p in SVM. 
%% gsSVMcgp: grid search algorithm for tuning parameters c,g,p in SVM. 
%% gsaSVMcgp: gravitational search algorithm for tuning parameters c,g,p in SVM.


%%About optimization algorithm's parameters:
For each optimization algorithms, there are some inner parameters, you can keep the default values or adjust them for better serving your application.


%% GNU GENERAL PUBLIC LICENSE V3.0
%% NOTE: HERE USE THE Chih-Jen Lin'S LIBSVM AS THE INTERNAL SVM/SVM.
%% NOTE: HERE USE THE GATBX AS TOOL FOR IMPLEMENTATION OF GENETIC ALGORITHM
%% OBTAIN libSVM HERE : http://www.csie.ntu.edu.tw/~cjlin/
%% ALSO, YOU CAN WRITE SUPPORT VECTOR MACHINE AS WELL AND CHANGE RELEVANT CODE FOR YOUR OWN APPLICATION.




